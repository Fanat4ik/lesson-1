package ru.geekbrains.DZ_1; // наш пакет

public class DZ_123 { // класс Main

    //3. Написать метод вычисляющий выражение a * (b + (c / d)) и возвращающий результат,
    //где a, b, c, d – аргументы этого метода, имеющие тип float.
    public static float result(float a, float b, float c, float d){ // Сам метод
        return a * (b + (c / d));
    }

    public static void main(String[] args) { // публичный(самый широкий спектр доступа), статическый(не требует создания объектов), ничего не возвращающий метод Main

        //2. Создать переменные всех пройденных типов данных и инициализировать их значения.
        //переменные целочисленного типа данных:
        byte b = -37;             // 8-битное знаковое целое число  от -128 до 127
        short sh = -377;          // 16-битное знаковое целое число от -32768 до 32767
        long l = -373737373L;     // 64-битное знаковое целое число от -2147483648  до 2147483647
        int i = -373737;          // 32-битное знаковое целое число от -9223372036854775808 до 9223372036854775807
        //переменные дробных числел типа данных:
        float f = 37.17f;         // 32-битное знаковое число с плавающей запятой одинарной точности
        double d = 478.952;       // 64-битное знаковое число с плавающей запятой двойной точности
        //переменная символьного типа данных:
        char ch = 'A';            // 16-битный тип данных, предназначенный для хранения символов в кодировке Unicode
        //переменная логического типа данных:
        boolean bool = true;      // принимает значение true или false

        //3...
        System.out.println((int)result(3.14f, 14.32f, 22.17f, 17.55f));


    }

}